function WaveObject(address, timestamp, message, lovedInSession){
    this.address = address
    this.timestamp = timestamp
    this.message = message
    this.lovedInSession = lovedInSession
}

export default WaveObject
