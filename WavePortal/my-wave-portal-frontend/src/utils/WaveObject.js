function WaveObject(address, timestamp, message, lovedInSession, id){
    this.address = address
    this.timestamp = timestamp
    this.message = message
    this.lovedInSession = lovedInSession
    this.id = id
}

export default WaveObject
